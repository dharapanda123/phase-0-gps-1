phase-0-gps-1
=============
git clone- clone new repo
touch-create new file
git add-adds the new repo
git commit- commits locally
git push-sernds to github
git checkout -b--create a branch
git checkout--help
